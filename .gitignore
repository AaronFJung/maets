# Dependencies (root Biome tooling installed via root package.json).
node_modules/

# OS / editor cruft.
# Per-folder build output and dependencies are ignored by next/.gitignore and
# supabase/.gitignore respectively.
.DS_Store
Thumbs.db
desktop.ini
*.log
